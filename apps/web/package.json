{
  "name": "@tokenboard/web",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode client && vite build",
    "preview": "wrangler dev",
    "deploy": "node scripts/prepare-production-config.mjs && node scripts/check-production-config.mjs && pnpm run build && wrangler deploy --config wrangler.production.jsonc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "private": true,
  "dependencies": {
    "@tokenboard/usage-core": "workspace:*",
    "better-auth": "^1.6.11",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.12.23",
    "honox": "0.1.55",
    "lucide": "^1.11.0",
    "tailwind-merge": "^3.5.0",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260527.1",
    "@hono/vite-build": "^1.11.1",
    "@hono/vite-dev-server": "^0.25.2",
    "@tailwindcss/vite": "^4.2.2",
    "drizzle-kit": "^0.31.8",
    "tailwindcss": "^4.2.2",
    "vite": "^8.0.9",
    "wrangler": "^4.95.0"
  }
}
